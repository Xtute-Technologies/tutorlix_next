pipeline {
    agent any

    // 1. Define Parameters
    parameters {
        choice(
            name: 'DEPLOY_SCOPE', 
            choices: ['All', 'Frontend', 'Backend'], 
            description: 'Choose which components to build and deploy.'
        )
    }
    environment {
        // We keep these for the initial checkout
        WORKSPACE_FRONTEND = "${WORKSPACE}/frontend"
        WORKSPACE_BACKEND  = "${WORKSPACE}/backend"

        // ✅ NEW: The actual Production Paths (Where the app lives)
        PROD_ROOT     = "/var/www/tutorlix-prod"
        PROD_FRONTEND = "/var/www/tutorlix-prod/frontend"
        PROD_BACKEND  = "/var/www/tutorlix-prod/backend"

        NEXT_APP_NAME = "tutorlix-next-prod" 
        NEXT_PORT     = "3700" 

        DJANGO_SETTINGS_MODULE = "tutorlix.settings"
        NEXT_PUBLIC_API_URL = "https://tutorlix.com"
    }

    stages {

        /* ================= CHECKOUT ================= */
        stage('Checkout') {
            steps {
                echo "🚀 Starting PROD deploy. Scope: ${params.DEPLOY_SCOPE}"
                checkout scm
            }
        }

        /* ================= SYNC FILES ================= */
        // We must move files from Jenkins Workspace to /var/www/tutorlix-prod
        stage('Deploy Files to Prod') {
            steps {
                echo "📂 Syncing files to ${PROD_ROOT}..."
                // Using rsync to copy files (excluding heavy/temp folders)
                // Ensure rsync is installed: sudo apt install rsync
                sh '''
                    mkdir -p $PROD_ROOT
                    
                    # Sync Frontend (Exclude node_modules to keep copies fast)
                    rsync -av --delete --exclude 'node_modules' --exclude '.next' --exclude '.git' $WORKSPACE_FRONTEND/ $PROD_FRONTEND/
                    
                    # Sync Backend (Exclude venv and pycache)
                    rsync -av --delete \
                        --exclude 'venv' \
                        --exclude '__pycache__' \
                        --exclude '*.pyc' \
                        --exclude '.git' \
                        --exclude 'db.sqlite3' \
                        --exclude 'media' \
                        $WORKSPACE_BACKEND/ $PROD_BACKEND/
                '''
            }
        }

        /* ================= FRONTEND STAGES ================= */
        
        stage('Build Next.js (CI)') {
            when { 
                expression { return params.DEPLOY_SCOPE == 'All' || params.DEPLOY_SCOPE == 'Frontend' } 
            }
            steps {
                sh '''
                    # ✅ Work in the PROD directory now
                    cd $PROD_FRONTEND

                    echo "📦 Installing deps (Clean Install)"
                    npm ci 

                    # Ensure PM2 is installed globally
                    sudo npm install -g pm2

                    echo "🏗️ Building Next.js with API URL: $NEXT_PUBLIC_API_URL"
                    export NODE_ENV=production
                    NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL npm run build
                '''
            }
        }

        stage('Start / Restart Next.js (PM2)') {
            when { 
                expression { return params.DEPLOY_SCOPE == 'All' || params.DEPLOY_SCOPE == 'Frontend' } 
            }
            steps {
                sh '''
                    # ✅ Work in the PROD directory
                    cd $PROD_FRONTEND
                    
                    PM2_CMD="sudo /usr/bin/pm2"
                    
                    echo "🔁 Managing PM2 Process: $NEXT_APP_NAME on Port $NEXT_PORT"

                    if $PM2_CMD list | grep -q "$NEXT_APP_NAME"; then
                        echo "✅ Process found. Reloading..."
                        sudo PORT=$NEXT_PORT $PM2_CMD reload $NEXT_APP_NAME --update-env
                    else
                        echo "✨ Process not found. Starting new..."
                        sudo PORT=$NEXT_PORT NODE_ENV=production $PM2_CMD start npm --name "$NEXT_APP_NAME" -- start
                    fi

                    $PM2_CMD save
                '''
            }
        }

        /* ================= BACKEND STAGES ================= */

        stage('Backend Setup') {
            when { 
                expression { return params.DEPLOY_SCOPE == 'All' || params.DEPLOY_SCOPE == 'Backend' } 
            }
            steps {
                sh '''
                    # ✅ Work in the PROD directory
                    cd $PROD_BACKEND

                    echo "🐍 Backend setup in $PROD_BACKEND"
                    
                    # 1. Clean and Create venv
                    rm -rf venv
                    python3 -m venv venv

                    # 2. Use the venv binaries DIRECTLY
                    ./venv/bin/pip install --upgrade pip
                    ./venv/bin/pip install -r requirements.txt
                    ./venv/bin/pip install gunicorn

                    export DJANGO_SETTINGS_MODULE=$DJANGO_SETTINGS_MODULE
                    
                    # 3. Migrate and Collect Static
                    ./venv/bin/python manage.py migrate || true
                    ./venv/bin/python manage.py collectstatic --noinput || true
                '''
            }
        }

        stage('Start / Restart Gunicorn') {
            when { 
                expression { return params.DEPLOY_SCOPE == 'All' || params.DEPLOY_SCOPE == 'Backend' } 
            }
            steps {
                sh '''
                    echo "🔁 Restarting Tutorlix Gunicorn Service"
                    sudo systemctl restart tutorlix-gunicorn-prod
                '''
            }
        }

        /* ================= NGINX ================= */
        stage('Reload Nginx') {
            steps {
                sh '''
                    echo "🔄 Reloading nginx configuration"
                    sudo nginx -t && sudo systemctl reload nginx
                '''
            }
        }
    }

    post {
        success {
            echo "✅ PROD Jenkins deploy ($params.DEPLOY_SCOPE) succeeded"
        }
        failure {
            echo "❌ PROD Jenkins deploy ($params.DEPLOY_SCOPE) failed"
        }
    }
}