pipeline {
    agent any

    options {
        timestamps()
        timeout(time: 45, unit: 'MINUTES')
    }

    environment {
        DOCKER_CREDS = credentials('dockerhub')
        KUBE_CONFIG = credentials('kubeconfig')
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Docker Login') {
            steps {
                sh '''
                    echo "$DOCKER_CREDS_PSW" | docker login -u "$DOCKER_CREDS_USR" --password-stdin
                '''
            }
        }

        stage('Build And Push Images') {
            parallel {
                stage('Backend Image') {
                    steps {
                        sh '''
                            set -e
                            echo "🚀 Building backend image..."
                            docker build \
                              --network=host \
                              -t "$DOCKER_CREDS_USR/tutorlix-backend:latest" \
                              ./backend

                            echo "📤 Pushing backend image..."
                            docker push "$DOCKER_CREDS_USR/tutorlix-backend:latest"
                        '''
                    }
                }

                stage('Frontend Image') {
                    steps {
                        sh '''
                            set -e
                            echo "🚀 Building frontend image..."
                            docker build \
                              --network=host \
                              -t "$DOCKER_CREDS_USR/tutorlix-frontend:latest" \
                              ./frontend

                            echo "📤 Pushing frontend image..."
                            docker push "$DOCKER_CREDS_USR/tutorlix-frontend:latest"
                        '''
                    }
                }
            }
        }

        stage('Setup Kubeconfig') {
            steps {
                withCredentials([file(credentialsId: 'kubeconfig', variable: 'KUBECONFIG_FILE')]) {
                    sh '''
                        set -e
                        mkdir -p "$HOME/.kube"
                        cp "$KUBECONFIG_FILE" "$HOME/.kube/config"
                        chmod 600 "$HOME/.kube/config"
                    '''
                }
            }
        }

        stage('Apply And Restart') {
            steps {
                sh '''
                    set -e
                    echo "📦 Applying Kubernetes manifests and restarting deployments..."
                    export KUBECONFIG="$HOME/.kube/config"

                    kubectl apply -k k8s/overlays/vps

                    kubectl rollout restart deployment/backend
                    kubectl rollout restart deployment/frontend
                '''
            }
        }

        stage('Verify Deployments') {
            parallel {
                stage('Backend Rollout And Migrate') {
                    steps {
                        sh '''
                            set -e
                            export KUBECONFIG="$HOME/.kube/config"

                            echo "⏳ Waiting for backend rollout..."
                            if ! kubectl rollout status deployment/backend --timeout=300s; then
                                echo "❌ Backend rollout timed out. Debug info:"
                                kubectl get deployment backend -o wide || true
                                kubectl describe deployment backend || true
                                kubectl get rs -l app=backend -o wide || true
                                kubectl get pods -l app=backend -o wide || true
                                kubectl describe pods -l app=backend || true
                                exit 1
                            fi

                            echo "✅ Backend rollout completed"

                            echo "🔍 Finding newest running backend pod..."
                            BACKEND_POD=$(kubectl get pods -l app=backend \
                              --field-selector=status.phase=Running \
                              --sort-by=.metadata.creationTimestamp \
                              -o jsonpath='{.items[-1:].metadata.name}')

                            if [ -z "$BACKEND_POD" ]; then
                                echo "❌ No running backend pod found for migrations"
                                kubectl get pods -l app=backend -o wide || true
                                exit 1
                            fi

                            echo "🗄️ Running migrations in pod: $BACKEND_POD"
                            kubectl exec "$BACKEND_POD" -- python manage.py migrate
                        '''
                    }
                }

                stage('Frontend Rollout') {
                    steps {
                        sh '''
                            set -e
                            export KUBECONFIG="$HOME/.kube/config"

                            echo "⏳ Waiting for frontend rollout..."
                            if ! kubectl rollout status deployment/frontend --timeout=300s; then
                                echo "❌ Frontend rollout timed out. Debug info:"
                                kubectl get deployment frontend -o wide || true
                                kubectl describe deployment frontend || true
                                kubectl get rs -l app=frontend -o wide || true
                                kubectl get pods -l app=frontend -o wide || true
                                kubectl describe pods -l app=frontend || true
                                exit 1
                            fi

                            echo "✅ Frontend rollout completed"
                        '''
                    }
                }
            }
        }
    }

    post {
        success {
            echo "✅ Deploy successful 🚀"
        }
        failure {
            echo "❌ Deploy failed"
        }
    }
}