pipeline {
    agent any

    options {
        timestamps()
        timeout(time: 30, unit: 'MINUTES')
        disableConcurrentBuilds()
    }

    environment {
        DOCKER_CREDS = credentials('dockerhub')
        KUBE_CONFIG = credentials('kubeconfig')
        DOCKER_BUILDKIT = '1'
        COMPOSE_DOCKER_CLI_BUILD = '1'
        BACKEND_IMAGE = 'ankitvashishta7/tutorlix-backend'
        FRONTEND_IMAGE = 'ankitvashishta7/tutorlix-frontend'
        IMAGE_TAG = "${BUILD_NUMBER}"
        BACKEND_SECRET_NAME = 'backend-env'
        OLLAMA_MODEL = 'qwen2.5:3b-instruct'
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Docker Login') {
            steps {
                sh '''
                    set -e
                    echo "$DOCKER_CREDS_PSW" | docker login -u "$DOCKER_CREDS_USR" --password-stdin
                '''
            }
        }

        stage('Build And Push Images') {
            parallel {
                stage('Backend Image') {
                    steps {
                        sh '''
                            set -e
                            echo "🚀 Building backend image..."
                            docker build \
                              --network=host \
                              -t "$BACKEND_IMAGE:$IMAGE_TAG" \
                              -t "$BACKEND_IMAGE:latest" \
                              ./backend

                            echo "📤 Pushing backend image..."
                            docker push "$BACKEND_IMAGE:$IMAGE_TAG"
                            docker push "$BACKEND_IMAGE:latest"

                            echo "🧹 Cleaning local backend image..."
                            docker image rm "$BACKEND_IMAGE:$IMAGE_TAG" "$BACKEND_IMAGE:latest" || true
                        '''
                    }
                }

                stage('Frontend Image') {
                    steps {
                        sh '''
                            set -e
                            echo "🚀 Building frontend image..."
                            docker build \
                              --network=host \
                              -t "$FRONTEND_IMAGE:$IMAGE_TAG" \
                              -t "$FRONTEND_IMAGE:latest" \
                              ./frontend

                            echo "📤 Pushing frontend image..."
                            docker push "$FRONTEND_IMAGE:$IMAGE_TAG"
                            docker push "$FRONTEND_IMAGE:latest"

                            echo "🧹 Cleaning local frontend image..."
                            docker image rm "$FRONTEND_IMAGE:$IMAGE_TAG" "$FRONTEND_IMAGE:latest" || true
                        '''
                    }
                }
            }
        }

        stage('Setup Kubeconfig') {
            steps {
                withCredentials([file(credentialsId: 'kubeconfig', variable: 'KUBECONFIG_FILE')]) {
                    sh '''
                        set -e
                        mkdir -p "$HOME/.kube"
                        cp "$KUBECONFIG_FILE" "$HOME/.kube/config"
                        chmod 600 "$HOME/.kube/config"
                    '''
                }
            }
        }

        stage('Ensure Ollama') {
            steps {
                sh '''
                    set -e
                    export KUBECONFIG="$HOME/.kube/config"

                    echo "🧠 Ensuring Ollama is installed..."
                    if command -v ollama >/dev/null 2>&1; then
                        ollama --version || true
                    else
                        curl -fsSL https://ollama.com/install.sh | sh
                    fi

                    echo "🚀 Ensuring Ollama service is running..."
                    if command -v systemctl >/dev/null 2>&1 && systemctl list-unit-files | grep -q '^ollama.service'; then
                        sudo systemctl enable ollama || true
                        sudo systemctl restart ollama || sudo systemctl start ollama || true
                    fi

                    if ! curl -fsS http://127.0.0.1:11434/api/version >/dev/null 2>&1; then
                        pkill -f "ollama serve" || true
                        nohup ollama serve >/tmp/ollama-dev.log 2>&1 &
                    fi

                    for i in $(seq 1 20); do
                        if curl -fsS http://127.0.0.1:11434/api/version >/dev/null 2>&1; then
                            break
                        fi
                        sleep 2
                    done

                    curl -fsS http://127.0.0.1:11434/api/version

                    echo "📦 Ensuring Ollama model exists..."
                    if ! ollama show "$OLLAMA_MODEL" >/dev/null 2>&1; then
                        ollama pull "$OLLAMA_MODEL"
                    fi

                    OLLAMA_NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="InternalIP")].address}')
                    if [ -z "$OLLAMA_NODE_IP" ]; then
                        echo "❌ Could not resolve Kubernetes node internal IP for Ollama"
                        exit 1
                    fi
                    echo "$OLLAMA_NODE_IP" > .ollama_node_ip_dev
                    echo "✅ Ollama will be exposed to backend via http://$OLLAMA_NODE_IP:11434"
                '''
            }
        }

        stage('Cluster Preflight') {
            steps {
                sh '''
                    set -e
                    export KUBECONFIG="$HOME/.kube/config"

                    echo "🔎 Cluster health check..."
                    kubectl get nodes -o wide

                    echo "🧹 Cleaning failed/evicted pods..."
                    kubectl delete pod --field-selector=status.phase=Failed --all-namespaces --ignore-not-found=true || true

                    echo "🧹 Cleaning completed pods..."
                    kubectl delete pod --field-selector=status.phase=Succeeded --all-namespaces --ignore-not-found=true || true

                    echo "📋 Recent node events..."
                    kubectl get events --sort-by=.lastTimestamp | tail -30 || true

                    if kubectl describe node | grep -qi 'disk-pressure'; then
                        echo "❌ Node still has DiskPressure. Free disk space first."
                        df -h || true
                        exit 1
                    fi
                '''
            }
        }

        stage('Apply Manifests And Update Images') {
            steps {
                sh '''
                    set -e
                    export KUBECONFIG="$HOME/.kube/config"

                    echo "📦 Applying manifests..."
                    kubectl apply -k k8s/overlays/vps

                    echo "🖼️ Updating images with immutable tags..."
                    kubectl set image deployment/backend backend="$BACKEND_IMAGE:$IMAGE_TAG"
                    kubectl set image deployment/frontend frontend="$FRONTEND_IMAGE:$IMAGE_TAG"
                '''
            }
        }

        stage('Sync Backend Secrets') {
            steps {
                withCredentials([
                    string(credentialsId: 'razorpay-secret-id-dev', variable: 'RAZORPAY_SECRET_ID'),
                    string(credentialsId: 'razorpay-secret-key-dev', variable: 'RAZORPAY_SECRET_KEY'),
                    string(credentialsId: 'razorpay-webhook-secret-dev', variable: 'RAZORPAY_WEBHOOK_SECRET'),
                    string(credentialsId: 'groq-api-key-dev', variable: 'GROQ_API_KEY')
                ]) {
                    sh '''
                        set -e
                        export KUBECONFIG="$HOME/.kube/config"

                        echo "🔐 Syncing backend dev secrets..."
                        OLLAMA_NODE_IP=$(cat .ollama_node_ip_dev)
                        kubectl create secret generic "$BACKEND_SECRET_NAME" \
                          --from-literal=RAZORPAY_SECRET_ID="$RAZORPAY_SECRET_ID" \
                          --from-literal=RAZORPAY_SECRET_KEY="$RAZORPAY_SECRET_KEY" \
                          --from-literal=RAZORPAY_WEBHOOK_SECRET="$RAZORPAY_WEBHOOK_SECRET" \
                          --from-literal=GROQ_API_KEY="$GROQ_API_KEY" \
                          --from-literal=OLLAMA_BASE_URL="http://$OLLAMA_NODE_IP:11434" \
                          --from-literal=OLLAMA_AI_TEACHER_MODEL="$OLLAMA_MODEL" \
                          --from-literal=FRONTEND_URL="https://dev.tutorlix.com" \
                          --dry-run=client -o yaml | kubectl apply -f -

                        kubectl set env deployment/backend --from=secret/"$BACKEND_SECRET_NAME"
                        kubectl describe secret "$BACKEND_SECRET_NAME" | sed -n '1,20p'
                    '''
                }
            }
        }

        stage('Verify Deployments') {
            parallel {
                stage('Backend Rollout And Migrate') {
                    steps {
                        sh '''
                            set -e
                            export KUBECONFIG="$HOME/.kube/config"

                            echo "⏳ Waiting for backend rollout..."
                            if ! kubectl rollout status deployment/backend --timeout=240s; then
                                echo "❌ Backend rollout failed. Debug info:"
                                kubectl get deployment backend -o wide || true
                                kubectl describe deployment backend || true
                                kubectl get rs -l app=backend -o wide || true
                                kubectl get pods -l app=backend -o wide || true
                                kubectl describe pods -l app=backend || true
                                kubectl get events --sort-by=.lastTimestamp | tail -40 || true
                                exit 1
                            fi

                            echo "🔍 Finding running backend pod..."
                            BACKEND_POD=$(kubectl get pods -l app=backend \
                              --field-selector=status.phase=Running \
                              --sort-by=.metadata.creationTimestamp \
                              -o jsonpath='{.items[-1:].metadata.name}')

                            if [ -z "$BACKEND_POD" ]; then
                                echo "❌ No running backend pod found"
                                kubectl get pods -l app=backend -o wide || true
                                exit 1
                            fi

                            echo "🗄️ Running migrations in $BACKEND_POD ..."
                            kubectl exec "$BACKEND_POD" -- python manage.py migrate
                        '''
                    }
                }

                stage('Frontend Rollout') {
                    steps {
                        sh '''
                            set -e
                            export KUBECONFIG="$HOME/.kube/config"

                            echo "⏳ Waiting for frontend rollout..."
                            if ! kubectl rollout status deployment/frontend --timeout=240s; then
                                echo "❌ Frontend rollout failed. Debug info:"
                                kubectl get deployment frontend -o wide || true
                                kubectl describe deployment frontend || true
                                kubectl get rs -l app=frontend -o wide || true
                                kubectl get pods -l app=frontend -o wide || true
                                kubectl describe pods -l app=frontend || true
                                kubectl get events --sort-by=.lastTimestamp | tail -40 || true
                                exit 1
                            fi
                        '''
                    }
                }
            }
        }
    }

    post {
        always {
            sh '''
                echo "🧹 Final Docker cleanup..."
                docker image prune -af || true
                docker builder prune -af || true
                docker volume prune -f || true
            '''
        }
        success {
            echo "✅ Deploy successful 🚀"
        }
        failure {
            echo "❌ Deploy failed"
        }
    }
}
