pipeline {
    agent any

    environment {
        DOCKER_CREDS = credentials('dockerhub')
        KUBE_CONFIG = credentials('kubeconfig')
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Images (NO CACHE)') {
            steps {
                sh '''
                    echo "🚀 Building Docker images (no cache)..."

                    docker build --no-cache \
                    --network=host \
                    -t $DOCKER_CREDS_USR/tutorlix-backend:latest \
                    ./backend

                    docker build --no-cache \
                    --network=host \
                    -t $DOCKER_CREDS_USR/tutorlix-frontend:latest \
                    ./frontend
                '''
            }
        }

        stage('Docker Login') {
            steps {
                sh '''
                    echo $DOCKER_CREDS_PSW | docker login -u $DOCKER_CREDS_USR --password-stdin
                '''
            }
        }

        stage('Push Images') {
            steps {
                sh '''
                    echo "📤 Pushing images..."

                    docker push $DOCKER_CREDS_USR/tutorlix-backend:latest
                    docker push $DOCKER_CREDS_USR/tutorlix-frontend:latest
                '''
            }
        }

        stage('Setup Kubeconfig') {
            steps {
                withCredentials([file(credentialsId: 'kubeconfig', variable: 'KUBECONFIG_FILE')]) {
                    sh '''
                        mkdir -p $HOME/.kube
                        cp $KUBECONFIG_FILE $HOME/.kube/config
                    '''
                }
            }
        }

        stage('Apply Kubernetes') {
            steps {
                sh '''
                    echo "📦 Applying Kubernetes manifests..."

                    export KUBECONFIG=$HOME/.kube/config

                    kubectl apply -k k8s/overlays/vps
                '''
            }
        }

        stage('Force Fresh Deployment') {
            steps {
                sh '''
                    echo "♻️ Forcing fresh pods (no cache)..."

                    export KUBECONFIG=$HOME/.kube/config

                    kubectl rollout restart deployment/backend
                    kubectl rollout restart deployment/frontend
                '''
            }
        }

        stage('Rollout Wait') {
            steps {
                sh '''
                    echo "⏳ Waiting for rollout..."

                    export KUBECONFIG=$HOME/.kube/config

                    kubectl rollout status deployment/backend
                    kubectl rollout status deployment/frontend
                '''
            }
        }
    }

    post {
        success {
            echo "✅ Deploy successful 🚀"
        }
        failure {
            echo "❌ Deploy failed"
        }
    }
}